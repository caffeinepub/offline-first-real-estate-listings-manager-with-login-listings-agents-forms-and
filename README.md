## Offline-first real estate listings manager with login, listings/agents forms, and Excel import/export

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
